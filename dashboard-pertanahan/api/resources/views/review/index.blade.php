@extends('layouts.app')
@section('content')
<div class="page-heading"><div><span class="eyebrow">Verifikasi data</span><h2>Review Data</h2><p>Hanya data berstatus submitted yang menunggu keputusan.</p></div><div class="review-count"><strong>{{ $revisions->total() }}</strong><span>Menunggu Review</span></div></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-error">{{ $errors->first() }}</div>@endif
<div class="panel">@if($revisions->isEmpty())<div class="empty-state"><div class="empty-icon">✓</div><h3>Tidak ada data menunggu review</h3><p>Data akan muncul setelah operator mengajukannya.</p></div>@else<div class="table-wrap"><table><thead><tr><th>Indikator</th><th>Wilayah</th><th>Periode</th><th>Sumber</th><th>Nilai</th><th>Pengirim</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($revisions as $revision)<tr><td><b>{{ $revision->observation->indicator->name }}</b></td><td>{{ $revision->observation->region->name }}</td><td>{{ $revision->observation->snapshot->as_of_date->format('d F Y') }}</td><td>{{ $revision->source->name ?? '-' }}</td><td>{{ $revision->value_decimal ?? $revision->value_integer ?? $revision->value_text ?? 'Data belum tersedia' }}</td><td>{{ $revision->created_by }}</td><td><span class="status-badge submitted">Diajukan</span></td><td><div class="review-actions"><form method="post" action="{{ route('review.publish',$revision) }}" data-confirm="Publikasikan data ini? Data akan tampil pada dashboard." data-submit-state>@csrf<button type="submit" class="primary-button">Publish</button></form><form method="post" action="{{ route('review.reject',$revision) }}" data-reject-form data-submit-state>@csrf<input name="reason" required placeholder="Alasan penolakan"><button type="submit" class="danger-button">Reject</button></form></div></td></tr>@endforeach</tbody></table></div>{{ $revisions->links() }}@endif</div>
@endsection
